Create a file called `AUTH_KEY.secret` using the `authkey` query parameter found in one of the animal details from Petango.

Be sure to be in this `scrape` folder, then...

```js
npm i
node scrape.js
```

**Note:** Data format may change!